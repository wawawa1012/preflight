# 给 DS 的 I6 施工提示词（整段复制）

你是 Preflight 唯一 current implementer。仓库 `F:\project\Preflight`（Windows 原生，Git Bash）。Grok 是 Architect，本切片不写代码，只在你交工后按测试输出 review。GLM 只读。任意时刻不要让第二个 AI 改同一工作区。

## 先读（冲突时按此顺序；用户后续指令优先于冻结蓝图）

`AGENTS.md` → `docs/PRODUCT_TREE.md` → `docs/MASTER_PLAN.md`（冻结，不得改）→ `docs/MILESTONES.md` → `docs/CONTRACTS.md` → `docs/UI_SPEC.md` → `README.md`

然后 **整份执行** 计划书，不要凭记忆减步骤：

`F:\project\Preflight\.hermes\plans\2026-09-18_150046-iteration-6.md`

用户已批准该计划。开工前 `git status` / `git log -1`，确认你是唯一写者。

## 本切片范围（I6 only）

报告中心 + 真实评分矩阵 + 带范围的「当前范围尚未发现引用」+ Evidence Drawer。
只读装配端点已批准（新契约 + 两个 GET，不改旧表、不改旧端点语义）。

**不要做：** Claim Inspector、数字矛盾、Overclaim、Grill、修复、Diff、第二模板、导出、搜索过滤、PDF/PPTX/DOCX、LLM 调用、FTS、覆盖率/已满足、首页「准备答辩」/Trust Layer。

## 战略（写进 PRODUCT_TREE，不要写成功能）

Controlled Breadth / 树冠优先：小时级一片叶子；不主动新建树干。Grill 五秒全句挂「Grill 迭代（预计 I9）」，禁止写「I7 全句」。候选 I7–I14 只更新账本/五问，本切片不实现。I13/I14 到点必须重新过准入三问。

## 计划书里已吸收的复核备注（必须遵守）

1. `assemble_report`：材料不存在返回 `None`，API 转 404。未绑定抛已有 `RubricNotBound`。不要抄任何 LookupError 玩笑行。
2. `ContractBundle` 必须显式包含 `MaterialPreflightCriterionRow`。
3. `assemble_summaries` 的 N+1 可接受，记入 CHANGELOG 已知限制；禁止为此加表/索引。
4. 报告页 409 未绑定态也要有回 Workbench 出口。导航不变量对所有状态生效。
5. 工作包 F **先改** `frontend/scripts/check-materials-nav.mjs` 的 Workbench fetch stub（按 URL 返回数组），再改 `WorkbenchView.vue`。只改页面会假红。

## 做法

- TDD：先红后绿再 commit。小步、单一主题。
- Python：`F:/project/Preflight/backend/.venv/Scripts/python.exe`，不要用 Hermes/系统 python。
- 契约改完必跑：`export_contracts.py` → `npm.cmd --prefix frontend run contracts` → `check_contracts.py`。
- `backend/.env`、`data/preflight.db`、`data/rubrics/*.json`、`.venv`、`node_modules` 永不入库。
- 不预测分数；界面只用计划书冻结词表。

## 交工

跑计划书「Tests / validation」全部命令，**原样粘贴输出**（unittest / check_contracts / build / 六个 check 脚本）。停下等人工验收。不要宣布完成、不要开 I7。
