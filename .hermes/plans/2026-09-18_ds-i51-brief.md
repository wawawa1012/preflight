# 给 DS 的 I5.1 施工提示词（整段复制）

你是 Preflight 唯一 current implementer。仓库 F:\project\Preflight（Windows 原生，Git Bash）。Grok 只 review。GLM 只读。不要让第二个 AI 同时写这个工作区。不要自行扩成 FTS/预过滤/向量库/temperature=0/性能重构。

先读：AGENTS.md → docs/PRODUCT_TREE.md → docs/MASTER_PLAN.md（冻结不改）→ docs/CONTRACTS.md → docs/UI_SPEC.md

然后整份执行，不要减步骤：
F:\project\Preflight\.hermes\plans\2026-09-18_163627-i51-proposal-quality.md

开工前 git status。工作区已有 MaterialReportView.vue「返回材料」未提交 diff——工作包 0 提交它，并给未绑定/error 卡片补 Workbench。禁止 history.back()。

本切片 I5.1 Proposal Quality：
1) 报告导航小 patch
2) LLM 日志（model/prompt_version/block_count/prompt_chars/elapsed_ms/candidate_count，有 usage 才记 token；不改契约）
3) Neg-Java + Pos-metrics fixture + stub runner（live 不进 CI，禁止代打用户 key）
4) Prompt v2：直接依据 vs 练习题/教程不算证据；空数组 [] 正常且优于牵强。PROMPT_VERSION=p5-criterion-preflight-v2。不要「最多 8 条」，不要 temperature=0
5) Proposal UI：「通过验证」改为「原文引用有效」；写明 AI 相关性待人判断；有 completed 提案时默认展示历史，按钮改「重新预检」才 POST
6) 报告/首页「已核证引用」改为「已确认关联」；详情「原文已校验」。JSON 字段 verified_citation_count 不改名
7) 文档与 check 脚本同步措辞

TDD、小步 commit、Python 用 backend/.venv。契约未改字段则不必 export；若只改文档/UI 仍跑全套 check。

交工：计划书验证命令输出原样粘贴。STOP。等用户 live benchmark。不要开 I5.2/latency。
