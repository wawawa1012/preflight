# 复制为 backend/.env 并填入任意 OpenAI 兼容服务；backend/.env 已被 Git 忽略。
PREFLIGHT_LLM_BASE_URL=https://api.example.com/v1
PREFLIGHT_LLM_API_KEY=sk-replace-me
PREFLIGHT_LLM_MODEL=replace-with-model-name
# 可选：请求超时秒数（默认 60）
PREFLIGHT_LLM_TIMEOUT_S=60
